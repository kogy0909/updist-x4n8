# 배포

최신 버전은 [Releases](../../releases)에서 받으세요.

## 설치

1. zip 다운로드 → 압축 해제
2. `install.bat` 실행 (최초 1회)
3. `autostart.vbs` 실행

## 업데이트

기존 폴더의 `config.json`, `profiles/`, `mascots/`는 그대로 두고
나머지 파일만 덮어쓰세요.
